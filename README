# Instructions on how to sync up the profile

cd ~
git clone http://github.com/fennellstyle/swiftfoxProfiles.git ~/.mozilla/firefox/
cd ~/.mozilla/firefox
#  add the actual profiles
git submodule init
git submodule update


